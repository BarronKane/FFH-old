#include <iostream>
#include <string>

int main()
{
    // TODO: Proper tests.
    // This executable will remain stubbed until implimentations mature.

    // Hack to fake a pause.
    std::string wait;
    std::cout << "Press enter to continue...";
    std::getline(std::cin, wait);
}
